# YOVUS - AI映像人物置換システム
# Requirements for RTX 5070 Laptop (8GB VRAM)

# ── Core ──
gradio>=4.44.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
tqdm>=4.65.0

# ── Video Processing ──
# ffmpeg-python>=0.2.0  # Optional: ffmpeg must be installed separately

# ── Face Detection & Swap ──
insightface>=0.7.3
onnxruntime-gpu>=1.16.0

# ── Face Enhancement ──
gfpgan>=1.3.8
basicsr>=1.4.2
realesrgan>=0.3.0

# ── Segmentation ──
rembg>=2.0.50
mediapipe>=0.10.0

# ── Pose Estimation ──
controlnet-aux>=0.0.7

# ── Body Generation (ControlNet + LoRA) ──
# Install separately due to CUDA version dependency:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
diffusers>=0.27.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0
xformers>=0.0.23

# ── GPU Monitoring ──
pynvml>=11.5.0

# ── Utilities ──
aiofiles>=23.0
httpx>=0.25.0
