# YOVUS v2.0 - AI映像人物置換システム
# Compatible with RTX 5070 Laptop (8GB VRAM)

# ── Core ──
gradio>=4.44.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
tqdm>=4.65.0

# ── Video ──
# FFmpeg required (install separately: winget install ffmpeg)

# ── Face Detection & Swap ──
insightface>=0.7.3
onnxruntime-gpu>=1.16.0

# ── Face Enhancement ──
gfpgan>=1.3.8
basicsr>=1.4.2
realesrgan>=0.3.0
facexlib>=0.3.0
codeformer-pip>=0.0.2

# ── Segmentation ──
rembg>=2.0.50
mediapipe==0.10.14  # 0.10.15+ removed mediapipe.solutions, breaks DWPose/OpenPose

# ── Pose Estimation ──
controlnet-aux>=0.0.7

# ── Generation (ControlNet + LoRA) ──
# PyTorch: install separately for your CUDA version:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
diffusers>=0.27.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0
peft>=0.7.0

# ── LoRA Training (8GB VRAM Optimization) ──
# bitsandbytes: Windows requires >=0.43.0 for native support
# Falls back to standard AdamW if unavailable (slightly higher VRAM)
bitsandbytes>=0.43.0

# ── GPU Monitoring ──
pynvml>=11.5.0

# ── Cloud APIs ──
replicate>=0.25.0
fal-client>=0.4.0
httpx>=0.25.0

# ── Utilities ──
aiofiles>=23.0
